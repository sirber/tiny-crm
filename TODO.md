# TODO

## Backend

- Router

  - Build a Security Context, with the current user id and roles, using the user token as reference
  - Decode the body into a DTO
  - Pass the security context and the dto to the services

- Service

  - Validate roles using the Security Context
  - Validate the DTO content

- Database

  - Parameters (create, update) and Returns should be a DTO

- DTO

  - add validations

- Tests

  - add unit tests
  - add end-to-end tests

## Frontend

pretty much everything! with react
